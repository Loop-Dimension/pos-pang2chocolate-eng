import 'package:flutter/material.dart';

class CounterTab extends StatelessWidget {
  const CounterTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('카운터'));
  }
}
