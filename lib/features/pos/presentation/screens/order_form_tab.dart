import 'package:flutter/material.dart';

class OrderFormTab extends StatelessWidget {
  const OrderFormTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('주문서'));
  }
}
